 <% if flash[:notice] %>
 <p style="color: limegreen;"><%= flash[:notice] %></p>
<% end %>


<h1>Editing Book</h1>
<% if @book.errors.any? %>

<h2><%= @book.errors.count %>errors prohibited this book from being saved:</h2>
    <ul>
        <% @book.errors.full_messages.each do |message| %>
            <li><%= message %></li>
        <% end %>
    </ul>
<% end %>
 <h4>Title</h4>
<%= form_with model:@book, local: true do |f| %>

<%= f.text_field :title %>
 <h4>body</h4>
 <%= f.text_field :body %>

 <br><%= f.submit 'Update Book' %>
 <br><%= link_to "show", book_path(@book) %>
  <%= link_to "Back", :books %>
  <% end %>
